import React from "react";
import { View } from "react-native";

export function SeparatorItem() {
  return (
    <View style={{ height: 3, width: '45%', backgroundColor: "#e8e8dc", alignSelf: 'center', borderRadius: 15}} />
  );
}